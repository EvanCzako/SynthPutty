<?xml version="1.0" encoding="UTF-8"?>
<!--
  One entry, because the app is one page: the keyboard, the controls and the
  spectrum panel are the same document, and a sitemap lists documents rather
  than views.

  A sitemap may only list URLs on the host that serves it, so the hub and the
  sibling apps are deliberately absent — each has its own. They are tied
  together by the domain property in Search Console, not by cross-listing.
-->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    <url>
        <loc>https://synthputty.evanczako.com/</loc>
        <changefreq>monthly</changefreq>
        <priority>1.0</priority>
    </url>
</urlset>
